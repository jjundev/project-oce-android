# 딸깍영어 (OneClickEng) — 앱 기능 상세 분석 보고서

> 분석 대상: `archive/android` (Android 네이티브 앱)
> 패키지: `com.jjundev.oneclickeng` · 버전 1.0.2 (versionCode 18)
> 규모: Java 159개 파일 · 약 41,000 LOC · 단위/계측 테스트 34개
> 작성일: 2026-06-29

---

## 1. 한 줄 요약

**딸깍영어**는 한국인 영어 학습자를 위한 **AI 기반 영어 회화·번역 학습 앱**이다. Google **Gemini** LLM으로 상황별 영어 대화 대본을 생성하고, 사용자가 마이크로 말한 발화를 분석(스피킹 피드백)하고, 한→영 번역을 첨삭(문장 피드백)하며, 세션이 끝나면 학습 내용을 요약하고 복습 퀴즈까지 출제하는 **end-to-end 자기주도 학습 루프**를 제공한다. 수익화는 **크레딧 기반**(인앱결제 + 리워드 광고 + 쿠폰)으로 이뤄진다.

---

## 2. 제품 정체성 & 대상

| 항목 | 내용 |
|------|------|
| 앱 이름 | 딸깍영어 (영문 코드명 OneClickEng) |
| 타깃 | 한국어 모국어 영어 학습자 (CEFR A1~C1 전 구간 대응) |
| 핵심 가치 | "딸깍" 한 번으로 AI가 상황 대화·첨삭·복습을 생성 |
| 학습 언어 | 모든 UI/피드백은 한국어(해요체), 학습 콘텐츠만 영어 |
| 최소 지원 | Android 12 (minSdk 31), 타깃 Android 16 (targetSdk 36) |

피드백 톤은 프롬프트 차원에서 엄격히 통제된다 — **해요체**, 문법 전문용어 금지, "혜택 우선(Benefit-First)" 화법, 모바일 스캔에 맞춘 2줄 이내 간결함, 교사가 아닌 파트너로서 공감·응원하는 태도.

---

## 3. 기술 스택

**언어/빌드**: Java 17, Gradle (AGP 8.13.2), Spotless(google-java-format) 코드 포매팅

**AI/네트워크**
- Google **Gemini API** (`generativelanguage.googleapis.com/v1beta`) — 텍스트 생성·음성 합성
- OkHttp 4.12 (SSE 스트리밍 포함), Gson 2.10

**Firebase/Google**
- Firebase **Auth**(인증), **Firestore**(클라우드 DB), **Cloud Messaging/FCM**(푸시), **Analytics**
- **Credential Manager** + Google ID (구글 로그인)
- Google **Play Billing** 8.3 (인앱결제), Play **App Update**(강제 업데이트), Play **Review**(인앱 리뷰)
- AdMob (`play-services-ads` 25.0) — 리워드 광고

**UI/미디어**
- AndroidX Navigation(단일 Activity + Fragment), ViewPager2, Material, ConstraintLayout, Flexbox, Preference
- **Media3 ExoPlayer** 1.9 — 영어 쇼츠 동영상 재생
- Lottie(애니메이션), Shimmer(스켈레톤 로딩), DynamicAnimation
- Lifecycle ViewModel/LiveData (MVVM)

---

## 4. 화면 구조 (5탭 하단 내비게이션)

단일 `MainActivity` + Navigation Component 기반. 하단 탭은 5개:

| 탭 | Fragment | 상태 | 설명 |
|----|----------|------|------|
| 🎬 **쇼츠** | `EnglishShortsFragment` | ✅ 구현 | 일일 영어 학습 숏폼 영상 |
| 🎮 **게임** | `GameModeSelectFragment` | ⚠️ **미구현(플레이스홀더)** | 알림함 버튼만 존재 |
| 📚 **학습** (시작 화면) | `LearningModeSelectFragment` | ✅ 구현 | 학습 모드 선택 → 대화 학습 진입 |
| 📜 **기록** | `LearningHistoryFragment` | ✅ 구현 | 저장한 단어/표현/문장, 복습 퀴즈 |
| ⚙️ **설정** | `SettingFragment` | ✅ 구현 | 프로필·모델·TTS·데이터 관리 |

별도 Activity: `LoginActivity`(런처), `DialogueLearningActivity`(대화 학습), `QuizActivity`(퀴즈).
크레딧 상점/내역(`CreditStoreFragment`, `CreditHistoryFragment`)은 설정·기록 탭에서 진입.

---

## 5. 핵심 기능 ① — AI 대화 학습 (Dialogue Learning) ★ 플래그십

앱의 중심 기능. 상황극(role-play) 형태로 진행된다.

### 5.1 대본 생성
- 사용자가 **레벨**(매우 쉬움~매우 어려움 = CEFR A1~C1), **주제**, **길이**(10/20/30줄)를 선택
- 99개의 추천 주제 프리셋 제공(`recommend_scripts.csv`) — 카페 주문, 회사 자기소개, 공항 입국심사, 택시, 식당 예약, 호텔 체크인, 병원 증상 설명, 환불 요청, 면접 등 실생활 상황
- `DialogueGenerateManager`가 Gemini에 요청 → **상대역(opponent)과 학습자(user)가 번갈아 말하는 대본**을 JSON으로 생성 (상대 이름·성별·역할 메타데이터 포함)
- 스트리밍(SSE)으로 받으며 `IncrementalDialogueScriptParser`가 **완성된 턴부터 즉시 화면에 렌더링** → 전체 응답을 기다리지 않음

### 5.2 턴 단위 학습 루프
한 턴(문장 쌍)마다 다음이 반복된다:

```
대본 다음 턴 결정 (LearningSessionOrchestrator)
   │
   ├─ [상대역 턴] → 채팅에 AI 메시지 표시 → TTS 음성 재생 → 자동으로 다음 턴
   │
   └─ [학습자 턴] → 한국어 문장 제시 ("이걸 영어로 말해보세요")
        │
        ▼ 사용자가 마이크 버튼 탭
      녹음 시작 (AudioRecorder, 16kHz PCM) + 실시간 파형(WaveformView) 표시
        │
        ▼ 정지 버튼 탭
      무음 감지 (RecordedAudioSilenceDetector, RMS < 0.03이면 재시도 유도)
        │
        ▼ 음성 있음
      ① 스피킹 분석 (SpeakingFeedbackManager) → 유창성/자신감/머뭇거림/전사(transcript)
        │
        ▼ 전사된 텍스트를 재사용
      ② 문장 피드백 (SentenceFeedbackManager, 스트리밍) → 문법·자연스러움·톤·패러프레이징
        │
        ▼ (선택) 추가 질문 (ExtraQuestionManager)
      사용자가 "다음" 탭 → 채팅에 기록 → 다음 턴으로 순환
```

> **성능 최적화**: 과거에는 오디오를 두 API에 병렬 전송(중복 음성인식)했으나, 스피킹 분석의 transcript를 문장 피드백에 **텍스트로 재사용**하는 순차 처리로 전환해 대기 시간을 8~15초 → 5~9초로 단축(`docs/optimization-plan-sequential-feedback.md`).

### 5.3 소프트웨어 아키텍처 (계층 분리)
대화 학습은 매우 정교한 다층 구조로 설계되어 있다:

- **Fragment** (`DialogueLearningFragment`): UI 생명주기, 권한, 렌더링, 이벤트 라우팅
- **ViewModel** (`DialogueLearningViewModel`): 상태(LiveData) 보유, 회전 시 채팅 유지, 흐름 위임
- **Controller** (4종): 개별 흐름 담당 — `ScriptFlowController`(대본 진행), `SpeakingFlowController`(발화 분석), `FeedbackFlowController`(문장 피드백 스트리밍), `ExtraQuestionFlowController`(추가 질문)
- **Coordinator** (6종): UI와 컨트롤러를 잇는 상태 기계 — `DialogueTurnCoordinator`(턴 진행), `DialogueSpeakingCoordinator`(녹음 생명주기), `DialoguePlaybackCoordinator`(TTS 재생), `DialogueFeedbackCoordinator`(피드백 UI·북마크), `SpeakingSceneCoordinator`(녹음 화면), `MicPermissionCoordinator`(권한)
- **Orchestrator** (`LearningSessionOrchestrator`): 4개 흐름을 종합해 "다음에 무엇을 할지" 중앙 결정. `AsyncRequestTracker`로 경쟁 요청(stale response) 차단

### 5.4 UI 씬(Scene)
하단 시트는 5개 모드로 전환된다: `DEFAULT_INPUT`(문장 제시) → `BEFORE_SPEAKING`(준비) → `WHILE_SPEAKING`(녹음·파형) → `FEEDBACK`(점수·첨삭) → `FINISHED`(완료·요약 CTA).

---

## 6. 핵심 기능 ② — AI 문장 피드백 (Sentence Feedback)

한국어 문장의 영어 번역을 **6단계로 첨삭**하는, 프롬프트가 가장 정교한 기능(시스템 프롬프트 711줄 + 레퍼런스 882줄). `SentenceFeedbackManager`가 섹션별 스트리밍으로 처리(`IncrementalJsonSectionParser`).

1. **작문 점수** (0~100) + 격려 메시지 + 점수별 색상(70↑ 초록 / 50–69 주황 / 50↓ 빨강)
2. **문법 교정**: 문장을 정상/오류(취소선)/교정/강조 세그먼트로 분해 + 혜택 중심 설명
3. **개념 브릿지(Conceptual Bridge)**: 사용자 영어를 한국어로 직역해 의미 격차를 보여줌 + **벤 다이어그램**으로 사용자 단어 vs 추천 단어의 의미 비교(전용 `VennDiagramView`, 라이트/다크 모드 대비 접근성 규칙 내장)
4. **자연스러움**: 원어민식 표현 + 정확히 2개의 이유(키워드 + 설명)
5. **톤/스타일 스펙트럼**: 5단계 격식(매우 격식 ~ 슬랭)을 영어+한국어로 제시
6. **패러프레이징**: 초·중·고급 3단계 대체 표현

프롬프트에는 한국인 학습자의 **8대 오류 유형**(SOV→SVO 어순, 관사, 복수형, 시제, 전치사, 주어 생략, 형용사/부사 혼동, 콩글리시)이 레퍼런스로 내장되어 있다.

---

## 7. 핵심 기능 ③ — 스피킹 피드백 (Speaking Feedback)

사용자의 **음성(WAV/PCM)을 Gemini에 전송**해 분석. `SpeakingFeedbackManager`가 처리하고 다음 JSON을 반환:
- `fluency` 유창성 (1~10), `confidence` 자신감 (1~10), `hesitations` 머뭇거림 횟수
- `transcript` 음성 인식 결과 (→ 문장 피드백으로 재사용)
- `feedback_message` 짧은 한국어 코칭 메시지

---

## 8. 핵심 기능 ④ — 세션 요약 (Session Summary)

대화 학습이 끝나면 `DialogueSummaryFragment`가 세션 전체를 정리한다. 스키마(`summary_fragment_payload.schema.json`):
- **totalScore**: 세션 종합 점수
- **highlights**: 가장 잘한 순간 (최대 1개)
- **expressions**: before/after 표현 개선 카드 (최대 8개)
- **words**: 신규 어휘 카드 (최대 12개)
- **likedSentences**: 사용자가 북마크한 패러프레이즈 문장 (최대 8개)
- **futureSelfFeedback**: 잘한 점/개선점 코칭

생성 파이프라인: 세션 중 누적된 피드백을 `SessionSummaryGenerator`가 후보로 가공(점수 90↑은 하이라이트, 그 외는 학습 재료로) → Gemini로 **어휘 추출**(이미 아는 단어·기초 단어 제외, 교육 가치순 정렬)과 **표현 필터링**(중복 제거, 점수 기반 엄격도 조절, "자연스러운 표현"/"정확한 표현" 2분류)을 거쳐 최종 카드 완성. 사용자는 단어/문장/표현을 **저장**해 학습 기록에 보관할 수 있다.

---

## 9. 핵심 기능 ⑤ — 복습 퀴즈 (Quiz)

저장한 학습 내용 기반으로 Gemini가 복습 퀴즈를 출제. `QuizGenerateManager`(스트리밍, `IncrementalQuizQuestionParser`).
- **진입 경로**: 세션 요약 직후, 또는 학습 기록 탭에서 단어/표현/문장별로
- **설정**: 출제 범위(최근 1주/2주/3주/3주↑), 문제 수(1~10)
- **문제 유형**: 오류 교정, 상황→영어, 교정 근거, 영↔한, 빈칸 채우기, 문장 번역 (한 유형 연속 2회 초과 금지)
- **진행**: 4지선다 또는 주관식 입력 → 정답 확인 → 해설 → 다음 → `QuizResultFragment`에서 정답률 표시

---

## 10. 영어 쇼츠 (English Shorts)

세로 스크롤 숏폼 동영상 학습 피드(틱톡式). `EnglishShortsFragment` + ViewPager2 + Media3 ExoPlayer(100MB LRU 캐시).
- **데이터**: Firestore `english_shorts` 컬렉션 실시간 리스너(`isActive==true`만), 제목·영상 URL·태그·좋아요/싫어요 수
- **상호작용**: 좋아요/싫어요(로그인 필요), 사용자별 반응을 `users/{uid}/english_shorts_likes|dislikes`에 기록
- **UI**: 상단 제목·태그(최대 3개) 오버레이, 하단 액션 레일, 다음 영상 프리로드

---

## 11. 학습 기록 & 게임화

### 11.1 학습 기록 (`LearningHistoryFragment`)
- **저장 카드**: 단어/표현/문장 3종 탭, 스와이프 삭제 지원
- Firestore `users/{uid}/saved_cards`에 저장 (`SavedCard` 모델)
- 카드 기반 복습 퀴즈 생성 진입점

### 11.2 게임화 지표 (로컬 + Firestore 동기화)
- **학습 포인트(XP)**: 난이도별 차등 — 초급 5 / 초중급 10 / 중급 20 / 중상급 35 / 고급 50점. 세션 ID 기반 **멱등 적립**(중복 방지). `LearningPointStore`(로컬) + `LearningPointCloudRepository`(Firestore 트랜잭션, 오프라인 대기열)
- **학습 시간**: 화면 노출 시간을 일별/누적 집계, 자정 넘김 처리. `LearningStudyTimeStore` + `LearningStudyTimeCloudRepository`
- **연속 학습(streak)**: "N일째 열공 중 🔥" 표시
- 대시보드는 `SlotMachineTextView`(슬롯머신식 숫자 카운트업 애니메이션)로 지표를 연출
- 숨은 이스터에그: 개발자/기획자 로고를 2초 내 5번 탭하면 보너스 학습시간·XP 지급

---

## 12. 크레딧 & 수익화 모델

학습 핵심 기능은 **크레딧을 소비**하며, 여러 경로로 충전한다.

### 12.1 크레딧 소비
| 기능 | 비용 |
|------|------|
| 대화 대본 생성 | 11줄 미만 **1크레딧**, 11줄 이상 **2크레딧** |
| 복습 퀴즈 생성 | 6문제 미만 **1크레딧**, 6문제 이상 **2크레딧** |

잔액은 Firestore `users/{uid}.credit`에서 확인 → 부족 시 충전 다이얼로그, 충분 시 차감 후 진행.

### 12.2 크레딧 충전
- **인앱결제** (Google Play 소비성 INAPP): `credit_10_v2`(10개/300원), `credit_20`(20개/500원), `credit_50`(50개/1000원)
- **리워드 광고**: 1회 시청당 **+1크레딧** (AdMob, 지수 백오프 재시도, 온디맨드 로딩)
- **쿠폰**: Firestore `coupons` 조회 → `reward_credit` 지급(1인 1회, `used_code` 배열로 중복 차단)
- **가입 보너스**: 최초 가입 시 **+5크레딧** (멱등, `signup_bonus_v1` 이벤트로 1회 보장)

### 12.3 결제 검증 파이프라인 (서버 신뢰 모델)
앱은 결제 후 **자체로 크레딧을 지급하지 않고** 백엔드로 검증을 위임:
1. Play 결제 완료 → 구매 토큰 획득
2. 백엔드 `CREDIT_BILLING_VERIFY_URL`에 Firebase ID 토큰 + 구매 정보 POST
3. 서버가 Google Play Developer API로 토큰 검증 → SKU→크레딧 매핑 → Firestore 트랜잭션으로 잔액 증가 + 불변 원장(`billing_ledger`) 기록
4. 응답 상태(`GRANTED`/`ALREADY_GRANTED`/`PENDING`/`REJECTED`/`INVALID`/`SERVER_ERROR`)에 따라 앱이 토큰 소비(consume) 또는 대기열 유지
5. 앱 시작 시 `BillingStartupAutoConsumeCoordinator`가 미처리/실패 구매를 자동 복구·재검증
6. 환불은 RTDN + Voided Purchases API로 감지해 크레딧 차감(클로백, 음수 잔액 허용)

### 12.4 크레딧 이용 내역
모든 변동은 Firestore `users/{uid}/credit_events`에 기록(이벤트·델타·잔액·사유·화면). 내역 화면은 구매 충전/광고 충전/학습 사용/퀴즈 사용/가입 보너스를 색상과 함께 표시.

---

## 13. 인증 & 사용자 데이터

### 13.1 로그인 (`LoginActivity`)
- **구글 로그인**: Credential Manager API (Gmail 주소는 자동으로 구글 로그인으로 유도)
- **이메일/비밀번호**: 이메일 존재 확인 → 로그인 또는 회원가입(6자+ 비밀번호)
- 배경에 무음 루프 영상, 바텀시트 슬라이드 UX
- 신규/신규 기기 사용자에게 가입 보너스 5크레딧 지급

### 13.2 클라우드 동기화 (Firestore 구조)
```
users/{uid}
 ├─ credit, nickname
 ├─ learning_metrics/points      (총 XP, 세션별 적립 내역)
 ├─ learning_metrics/study_time  (학습시간·학습일·연속일)
 ├─ saved_cards/{cardId}         (저장 단어/표현/문장)
 ├─ credit_events/{eventId}      (크레딧 변동 원장)
 └─ english_shorts_likes|dislikes/{shortId}
english_shorts/{docId}           (숏폼 콘텐츠)
coupons, billing_purchases, billing_ledger (서버 소유)
```
로컬(SharedPreferences)은 오프라인 대기열·캐시 역할이며, 트랜잭션으로 클라우드와 병합(max/union 전략).

---

## 14. 알림 (Notifications)

- **FCM 푸시**: `OneClickMessagingService`가 **data-only 메시지**를 수신(전경/배경 일관 처리 보장). title/body/channelId/sentAt 키 지원
- **인앱 알림함**: 수신 알림을 `AppNotificationStore`(SharedPreferences, 사용자별 최대 100개)에 저장 → `NotificationInboxDialog`로 열람. 출처(푸시/앱/미표시) 구분
- Android 13+ POST_NOTIFICATIONS 권한 요청 처리

---

## 15. AI 엔진 아키텍처 (Gemini 연동)

### 15.1 매니저 6종
| 매니저 | 용도 | 스트리밍 | 캐시 |
|--------|------|:---:|:---:|
| `DialogueGenerateManager` | 대화 대본 생성 | SSE | 1h |
| `QuizGenerateManager` | 복습 퀴즈 생성 | SSE | 1h |
| `SentenceFeedbackManager` | 문장 6단계 첨삭 | SSE | 1h |
| `SpeakingFeedbackManager` | 음성 분석 | — | 1h |
| `ExtraQuestionManager` | 추가 질문(평문) | SSE | — |
| `GeminiTtsManager` | 음성 합성(TTS) | — | — |

- **모델**: 텍스트는 Gemini Flash 계열(코드 기본값 `gemini-3.1-flash-lite-preview`), 음성은 `gemini-2.5-flash-preview-tts`. 설정 화면에 기능별 모델 프리셋(`gemini-3-flash-preview`/`gemini-2.5-flash-lite`/`gemini-2.5-flash`)이 노출되나, 사용자 모델 오버라이드는 현재 스텁(읽기 전용) 상태
- **API 키**: `BuildConfig.GEMINI_API_KEY` (쿼리 파라미터)
- **TTS**: 기본 음성 "Kore", PCM(기본 24kHz) base64 반환 → `RecordingAudioPlayer`(AudioTrack)로 재생. 단말 내장 Android TTS와 서버 기반 Gemini TTS를 설정에서 선택

### 15.2 점진적 스트리밍 파서 (3종)
부분 JSON을 표준 파서에 넣으면 깨지는 문제를 해결하기 위해, **중괄호 깊이를 추적해 완성된 객체만 추출**하는 자체 파서를 둠:
- `IncrementalDialogueScriptParser` — 대본 턴을 도착 즉시 렌더링
- `IncrementalQuizQuestionParser` — 퀴즈 문제 단위 추출
- `IncrementalJsonSectionParser` — 문장 피드백 6개 섹션을 섹션 단위로 추출

### 15.3 프롬프트 캐싱
시스템 프롬프트 + 컨텍스트를 Gemini **Prompt Caching API**(1시간 TTL)에 올려 토큰 비용 절감. SharedPreferences에 캐시 이름·생성시각을 저장해 앱 재시작 시 서버 검증 후 재사용(`docs/cache-persistence-plan.md`).

---

## 16. 설정 (Settings)

- **프로필**: 닉네임(클라우드 동기화), 이메일, 비밀번호 변경, 쿠폰 입력, 로그아웃
- **AI 모델**: 기능별 Gemini 모델 선택 + 연결 테스트 (오버라이드는 현재 스텁)
- **TTS**: 제공자(Android 고정), 말하기 속도(0.5~1.5x), 로케일 프리셋(en-US/GB, ko-KR, ja-JP)
- **음소거**: 전체 재생 음소거
- **데이터 관리**:
  - 학습 데이터 정리 — 보존 기간 선택(1주/2주/1개월/3개월/전체 삭제), Firestore 배치 삭제
  - 누적 기록 초기화 — 연속 학습일/누적 시간/XP 선택 초기화
- **앱 정보**: 버전, 버전 코드

---

## 17. 커스텀 UI 컴포넌트

- `VennDiagramView` — 문장 피드백의 어휘 비교 벤 다이어그램 (라이트/다크 대비 보장)
- `WaveformView` — 녹음 중 실시간 음성 파형 (40개 바)
- `SlotMachineTextView` / `SlotMachineTextGroupView` — 대시보드 지표의 슬롯머신식 카운트업 애니메이션
- Shimmer 스켈레톤 — 대본/퀴즈/피드백 로딩 중 플레이스홀더

---

## 18. 미구현 / 준비 중

- **게임 모드 탭**: 플레이스홀더(알림함 버튼만). 게임 로직·UI 없음
- **사용자 Gemini API 키/모델 오버라이드**: 설정 UI는 있으나 저장 로직은 스텁
- **API 기반 TTS**: 설정에 "준비 중"으로 표기

---

## 19. 엔지니어링 품질 평가

- **성숙도**: 단순 데모가 아닌 **상용 출시 수준**. 인증·결제·검증·환불·클라우드 동기화·푸시·인앱 업데이트·리뷰 유도까지 갖춤
- **아키텍처**: 대화 학습은 Fragment/ViewModel/Controller/Coordinator/Orchestrator 5계층으로 책임을 명확히 분리한 정교한 MVVM. 인터페이스(`I*Manager`)로 AI 매니저를 추상화해 테스트 용이성 확보
- **테스트**: 파서·결제 검증·요약 생성·학습 지표·위젯 등 34개 단위/계측 테스트 보유
- **신뢰성 설계**: 멱등 적립, 비동기 경쟁 차단(AsyncRequestTracker), 결제 토큰 대기열 자동 복구, 무음 감지, TTS 워치독, 토큰 기반 콜백 보호 등 엣지케이스 다수 처리
- **비용 의식**: 스트리밍 점진 렌더링 + 프롬프트 캐싱 + 순차 처리로 LLM 비용·지연 최적화
- **AI 활용 흔적**: `AGENTS.md`/`GEMINI.md`에 AI 에이전트의 작업 실수 회고가 누적 기록됨 — AI 페어 프로그래밍으로 개발된 프로젝트

### 보완 여지
- 게임 모드 미완성, 모델 오버라이드 스텁 → 설정 UI와 실제 동작 불일치 가능
- 쿠폰 지급은 `CreditHistoryEventLogger`를 거치지 않아 크레딧 내역에 자동 기록되지 않음
- 백엔드(결제 검증 서버, Firestore 보안 규칙)는 이 저장소 밖에 있어 별도 검토 필요

---

## 20. 결론

딸깍영어는 **"AI가 만든 상황 대화로 말하고 → 발음·번역을 즉시 첨삭받고 → 요약·퀴즈로 복습하는"** 완결된 영어 학습 루프를 Gemini LLM 위에 구현한 앱이다. 크레딧 기반 수익화와 게임화로 리텐션을 노리며, 스트리밍·캐싱·계층형 아키텍처 등 엔지니어링 완성도가 높다. 핵심 기능은 모두 동작하며, 게임 모드만 향후 확장 영역으로 남아 있다.
