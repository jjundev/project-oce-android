# 딸깍영어 (OneClickEng) — 제품 요구사항 정의서 (PRD)

> **버전:** v1.0 (Confirmed) · **작성일:** 2026-06-29 (확정 2026-06-30) · **상태:** grill-review(deep auto) 반영 + OQ1~4 전부 해결 + 상세 설계 산출물 완비(데이터·TTS·프롬프트·디자인 토픽·디자인 토큰)
> **대상 릴리즈:** v1 (완전 재구축, Android)
> **근거:** 기존 앱 분석(`딸깍영어_앱_분석_보고서.md`) + UI/UX 분석(`딸깍영어_UIUX_분석_보고서.md`) + 제품 인터뷰 15문항
> **개정:** v0.1 인터뷰 초안 → v0.2 객관 리뷰 수정 반영 (Blocker 4 + Advisory 10 해소, 성공 게이트 D7 20% 확정) → v0.3 OQ4(Firestore 데이터 스키마·보안 규칙) 설계 확정 → **v1.0 상세 설계 산출물 완비로 Confirmed 격상** (프롬프트 시스템·디자인 토큰 문서 추가, OQ1~4 전부 해결). 핵심 결정 요약은 부록 A, 변경 이력은 부록 B 참조.

---

## 1. 한 줄 요약

딸깍영어 v1은 **말하기에 자신 없는 한국인 기초~중급(A2~B1) 성인**이, AI가 만든 상황 대본을 따라 **부담 없이 영어로 말해보고 즉시 첨삭받는** 경험을 제공하는 Android 앱이다. 기존 앱을 **모던 스택(Kotlin·Compose)으로 처음부터 재구축**하되, 사방으로 퍼졌던 기능을 **핵심 학습 루프(대화 → 피드백 → 요약)** 하나에 재집중한다.

---

## 2. 배경 & 재구축 동기

기존 딸깍영어(v1.0.2)는 Java 17 + XML View + Material 2 기반의 약 41,000 LOC 상용 앱으로, 대화 학습·피드백·요약·퀴즈·쇼츠·게임화·크레딧 결제까지 갖춘 성숙한 제품이었다. 그러나:

- **기술 부채**: Java/XML/M2는 모던 Android 표준(Kotlin/Compose/M3)과 격차가 크고, 자체 디자인 가이드조차 Compose/M3를 *지향점*으로만 적어둔 상태였다.
- **스코프 분산**: 진짜 차별점인 AI 대화·첨삭 루프 외에, 운영 비용이 큰 부속물(영상 콘텐츠 파이프라인, 미구현 게임 탭, 정교한 크레딧/결제/환불 장치)이 제품을 무겁게 했다.
- **보안 구멍**: Gemini API 키를 클라이언트(`BuildConfig`)에 박아 직접 호출 → 키 추출·비용 남용 위험.

**재구축의 성격은 "기능 완전 동등 이식"이 아니라 "기술 현대화 + 핵심 루프 재집중"이다.** 동작하는 41k LOC를 복제하는 것이 아니라, 핵심을 골라 모던 스택 위에 탁월하게 다시 짓는다.

---

## 3. 목표 & 비목표

### 3.1 목표 (Goals)
- **G1.** 불안한 초·중급 학습자가 첫 세션에서 "나 영어로 말했다!"는 성공 경험을 하게 한다.
- **G2.** 대화 → 피드백 → 요약의 핵심 루프를 **빠르고(리듬 있는 회화) 깊이 있게(차별적 첨삭)** 완성한다.
- **G3.** 모던하고 유지보수 가능한 코드베이스(Kotlin/Compose/M3 + 안전한 백엔드 프록시)를 만든다.
- **G4.** 습관 형성을 통해 **재방문(리텐션)** 을 확보한다.

### 3.2 비목표 (Non-Goals, v1)
- **NG1.** 수익화(결제/구독/광고/크레딧). → v1.1 이후 구독으로. (v1은 비용 통제용 일일 한도만 두며, 과금 UI는 출하하지 않는다 — FR-26 참조.)
- **NG2.** iOS / 크로스플랫폼. → Android 전용.
- **NG3.** 영어 쇼츠(영상 콘텐츠), 게임 모드 탭. → 컷.
- **NG4.** 복습 퀴즈, FCM 서버 푸시. → v1.1 보류.
- **NG5.** 자유 대화(open-ended) 모드. → 대본형 롤플레이로 한정.
- **NG6.** 다국어 UI, WCAG AAA 전수 준수, 머티리얼 유 다이내믹 컬러.

---

## 4. 타깃 사용자 & 페르소나

| 항목 | 내용 |
|---|---|
| **핵심 페르소나** | "남들 앞에서 영어로 말하기가 무서운" **기초~중급(CEFR A2~B1) 성인** — 직장인·취준생 등 |
| **핵심 동기** | 말하기 울렁증 해소. 혼자, 부담 없이, 안전하게 말하기 연습 |
| **제외(후순위)** | 완전 입문자(A1, 문장 생성 자체가 어려움) / 고급자(C1, 이런 첨삭이 시시함) |
| **언어 환경** | 모국어 한국어. UI·피드백은 100% 한국어(해요체), 학습 콘텐츠만 영어 |

**설계 원칙(이 페르소나에서 도출):** 명확한 발판 제공(백지 공포 제거) · 비난 없는 톤 · 작은 성공의 연속 · 인지 과부하 최소화.

---

## 5. 제품 원칙 (Product Principles)

1. **안전이 먼저다.** 틀려도 괜찮은 분위기. 비난 없는 에러, 공감 화법(해요체), 격려 우선.
2. **회화엔 리듬이 있다.** 한 마디 → 짧은 피드백 → 다음 마디. 깊은 분석은 사후 복기로 미룬다.
3. **딸깍 한 번.** 복잡한 설정 없이 한 번의 동작으로 완성된 학습 세션에 진입.
4. **명확한 발판.** "이 한국어를 영어로"라는 분명한 과제로 백지 공포를 없앤다.
5. **돈은 정직하게, 나중에.** v1은 검증에 집중. 다크패턴·강매 없음.

---

## 6. 성공 지표

| 층위 | 지표 | v1 목표 | 의미 |
|---|---|---|---|
| **북극성** | **D7 리텐션** (가입 후 7일째 돌아온 비율) | **20% (확정 게이트)** | 습관이 붙었는가 — 이 앱의 사활 |
| 활성화 | 첫 세션 완주율 | ~70% (잠정) | 첫 경험이 성공이었는가 |
| 깊이 | 주당 세션 수(활성자) | ~3 (잠정) | 얼마나 깊이 쓰는가 |
| 학습 가치(대용) | 세션당 저장 카드 수, 재방문 복기율 | 관찰 | 가치를 느꼈는가 |

> **"검증됨"의 정의 (수익화 게이트):** 베타 첫 안정 코호트(약 4주) 기준 **D7 리텐션 20% 도달** → v1.1 수익화 진입 신호. 북극성 목표(20%)는 확정값이며, 보조지표(완주율·주당 세션)는 코호트 데이터로 보정한다.

---

## 7. 범위 (Scope) — v1 경계선

| 기능 | v1 | 비고 |
|---|:---:|---|
| 대화 학습 (대본형 롤플레이) | ✅ 핵심 | 플래그십 |
| 문장 피드백 (턴=슬림 / 요약=깊이) | ✅ 핵심 | 차별점 |
| 스피킹 분석 (한 줄 격려, 숫자 점수 없음) | ✅ 핵심 | 정서 케어 |
| 세션 요약 | ✅ 핵심 | 깊은 분석의 무대 |
| 상대역 TTS 음성 | ✅ 핵심 | 회화 몰입 |
| 온보딩 (보장된 첫 세션) | ✅ 핵심 | 활성화 |
| 학습 기록 / 저장 카드 | ✅ 지원(간소화) | 배운 것 보관 |
| 게임화 (스트릭·학습시간·XP) | ✅ 지원(간소화) | 리텐션, 저비용 |
| 설정 | ✅ 최소 | 프로필·TTS·데이터관리 |
| 로컬 학습 리마인더 | ✅ 최소 | 로컬 알림만 |
| 복습 퀴즈 | ⏳ v1.1 | 저장 카드 위에 |
| FCM 서버 푸시 / 인앱 알림함 | ⏳ v1.1 | 백엔드 운영 필요 |
| 영어 쇼츠 | ❌ 컷 | 별도 사업 |
| 게임 모드 탭 | ❌ 컷 | 기존도 미구현 |
| 크레딧·인앱결제·리워드 광고·쿠폰 | ❌ 컷 | 수익화 보류 |
| 이메일/비번 로그인 | ❌ 컷 | Google 전용 |

---

## 8. 핵심 사용자 경험 & 주요 플로우

### 8.1 온보딩 — "보장된 승리" 첫 세션
**목표:** 첫 60초 안에 성공 경험. (지표: 첫 세션 완주율)

```
앱 실행 → (게스트로 자동 시작, 로그인 없음)
   │
   ▼ 초경량 2문항
   ① "지금 영어, 어느 정도세요?"  → 쉬움 / 보통 / 어려움
       (답은 profile.level에 저장 → 세션 #2부터 적용. 첫 세션은 무조건 '쉬움'으로 강제)
   ② "어떤 상황이 궁금해요?"      → 핵심 상황 카드 중 1개 선택 (예: 카페 주문)
   │
   ▼ 보장된 첫 세션 (5턴 고정, 쉬운 문장, 격려 2배)
   첫 대화 완주 → "어? 나 했다!" 순간 → 작은 요약 + 다음 유도
   │
   ▼ (첫 성공 이후) 비로소 제안: "진도를 저장할까요? → Google로 계속하기"
```

- 회원가입·기능 투어·권한 요청은 **첫 성공 이후**로 미룬다.
- 마이크 권한은 첫 "말하기" 직전에 맥락과 함께 요청.
- 온보딩 레벨 응답은 폐기하지 않고 `profile.level`에 저장해 세션 #2부터 기본 난이도로 쓴다.

### 8.2 핵심 루프 — 대화 학습 (대본형 롤플레이)

**8.2.1 대본 생성**
- 입력: 레벨(쉬움/보통/어려움) · 주제(로컬 큐레이션 300개·4그룹 + 직접 입력) · 길이(5/10턴, 첫 세션 5턴 고정).
- 백엔드가 Gemini로 **상대역(opponent)과 학습자(user)가 번갈아 말하는 대본**을 생성(상대역 이름·성별·역할 메타 포함).
- **스트리밍(SSE):** 백엔드가 완성된 턴을 SSE 이벤트로 보내면 클라이언트는 **파싱 없이 즉시 렌더**한다(점진 파싱은 백엔드 책임 — §10.1 참조). 전체 응답 대기 없음.

**8.2.2 턴 단위 루프**
```
다음 턴 결정
 ├─ [상대역 턴] → 채팅 말풍선 표시 → TTS 음성 재생 → 자동 다음 턴
 └─ [학습자 턴] → 한국어 문장 제시("이걸 영어로 말해보세요")
       │ 마이크 탭 → 녹음(16kHz PCM) + 실시간 파형
       │ 정지 탭 → 무음 감지(너무 조용하면 재시도 유도)
       ▼ 음성 있음
     ① 스피킹 분석: 백엔드가 Gemini 네이티브 오디오로 전사(transcript) + 한 줄 격려
       │ (숫자 점수 없음 — 정성 격려만; transcript를 텍스트로 재사용 → 중복 인식 없음)
       ▼
     ② 슬림 피드백(스트리밍): 작문 점수 + 문법 교정 + 더 자연스러운 표현 1개
       │ "더 보기" 탭 시에만 깊은 분석(개념 브릿지·벤다이어그램·톤·패러프레이징) 펼침
       ▼
     "다음" 탭 → 채팅에 기록 → 다음 턴
```

**8.2.3 음성 UI 4상태** (시그니처, Compose로 재구현)
`Ready`(회색 동심원) → `Recording`(핑크/빨강 + 리플 3겹) → `Analyzing`(블루그레이 + 프로그레스 링) → `Complete`(초록). 사용자가 "들을 차례/말할 차례/분석 중"을 색·형태만으로 인지.

### 8.3 세션 요약 (깊은 분석의 무대)
대화 종료 시 세션 전체를 정리. **턴에서 감췄던 깊이를 여기서 펼친다.**
- **종합 점수**(대형 숫자) + 격려
- **하이라이트**: 가장 잘한 순간 (최대 1)
- **표현 개선 카드**: before/after (최대 8)
- **신규 단어 카드**: 교육 가치순 (최대 12)
- **북마크 문장**: 사용자가 저장한 패러프레이즈 (최대 8)
- **코칭**: 잘한 점/개선점
- 사용자는 단어/표현/문장을 **저장**해 학습 기록에 보관.
- 생성 파이프라인: 세션 중 누적 피드백을 백엔드가 후보로 가공 → Gemini로 어휘 추출·표현 필터링(중복 제거, 점수 기반 엄격도) → 최종 카드.

> **점수 정책:** 표시되는 모든 숫자 점수는 **텍스트 기반 작문 점수**(및 그 합성인 종합 점수)다. **음성/prosody에 대한 숫자 점수는 v1에 없다** — 스피킹은 정성 "한 줄 격려"만 제공한다(R3 위험을 설계로 회피).

### 8.4 학습 기록 & 게임화
- **저장 카드**: 단어/표현/문장 3종 탭, 스와이프 삭제. (v1.1 퀴즈의 입력원)
- **게임화**: 학습 포인트(XP, 난이도별 차등, **세션 ID 기반 멱등 적립**) · 학습 시간(일별/누적) · 연속 학습(streak, "N일째 열공 중 🔥"). 슬롯머신 카운트업 연출 유지.
- 클라우드 동기화는 **§10.4의 명시적 per-field 정책**을 따른다(진실원천=서버, 멱등 원장/union/tombstone). 구 앱의 범용 다중 머지 전략은 폐기한다.

---

## 9. 기능 요구사항 (FR)

### 9.1 온보딩 & 인증
- **FR-1.** 앱 최초 실행 시 로그인 없이 **게스트(Firebase Anonymous)** 로 자동 시작한다.
- **FR-2.** 2문항(레벨·관심 상황) 입력 후 즉시 첫 세션에 진입한다. 첫 세션은 난이도 '쉬움', 길이 5턴으로 강제한다. 온보딩에서 받은 레벨은 `profile.level`에 저장되어 **세션 #2부터 적용**된다.
- **FR-3.** 첫 세션 완주 후에만 계정 저장(**Google 로그인**)을 제안한다. "Google로 계속하기" 시 `linkWithCredential`을 시도하며, 결과에 따라 FR-3a / FR-3b로 분기한다.
  - **FR-3a.** 성공(신규 신원): 익명 UID가 인플레이스로 승격되어 게스트 데이터(저장 카드·진도·게임화)가 **자동 보존**된다.
  - **FR-3b.** 충돌(`credential-already-in-use` = 기존 계정 보유 복귀 사용자): 기존 계정으로 로그인한 뒤, 게스트 UID의 `saved_cards`·`gamification`을 **§10.4 멱등 머지 정책으로 기존 계정에 명시 이관**하고 게스트 doc을 폐기한다. (`usage` 쿼터는 계정별이므로 이관하지 않는다.)
- **FR-4.** 이메일/비밀번호 로그인은 제공하지 않는다.

### 9.2 대화 학습
- **FR-5.** 레벨·주제·길이를 선택해 대본 생성을 요청한다. 주제는 로컬 큐레이션 300개(4그룹, 온보딩 6개) + 직접 입력 + 추천 새로고침을 지원한다.
- **FR-6.** 대본은 스트리밍으로 수신하며, 백엔드가 보낸 완성된 턴을 도착 즉시 렌더한다(클라이언트는 원시 JSON 파싱 안 함).
- **FR-7.** 상대역 턴은 채팅 표시 + TTS 재생 후 자동 진행한다.
- **FR-8.** 학습자 턴은 한국어 문장을 제시하고, 마이크 녹음(16kHz PCM) + 실시간 파형 + 무음 감지를 제공한다.
- **FR-9.** "채팅으로 입력하기"(텍스트 대체 입력)를 제공한다.
- **FR-10.** 녹음 후 스피킹 분석(전사 + 한 줄 격려, **숫자 점수 없음**)과 슬림 문장 피드백(**텍스트 작문 점수** + 문법 교정 + 자연스러운 표현 1개)을 순차 제공한다.
- **FR-11.** "더 보기"로 깊은 분석(개념 브릿지 + 벤다이어그램, 톤 스펙트럼, 다단계 패러프레이징)을 온디맨드로 펼친다.
- **FR-12.** 음성 UI는 4상태(Ready/Recording/Analyzing/Complete)를 시각적으로 구분한다.
- **FR-13.** 회전·백그라운드 복귀 시 채팅·진행 상태를 유지한다(상태 보존).
- **FR-14.** 경쟁 요청(stale response) 차단: 이전 턴의 늦은 응답이 현재 UI를 오염시키지 않는다.

### 9.3 세션 요약 & 저장
- **FR-15.** 세션 종료 시 종합 점수·하이라이트·표현/단어 카드·북마크·코칭을 점진(스켈레톤) 렌더한다.
- **FR-16.** 사용자는 단어/표현/문장 카드를 저장한다.
- **FR-17.** 저장 카드는 기록 탭에서 3종 탭 + 스와이프 삭제로 관리한다.

### 9.4 게임화
- **FR-18.** 세션 완료 시 난이도별 XP를 **멱등 적립**(세션 ID 기준 중복 방지)한다.
- **FR-19.** 일별/누적 학습 시간과 연속 학습일(streak)을 집계·표시한다.
- **FR-20.** 대시보드 지표는 슬롯머신 카운트업으로 연출한다.

### 9.5 설정 & 데이터 관리
- **FR-21.** 프로필(닉네임), TTS(**음질 토글 1개**: 자연스러움-서버/빠름-단말 · 말하기 속도), 전체 음소거를 설정한다. (제공자·음성·로케일은 코드 고정·비노출 — [docs/design/tts.md](docs/design/tts.md).)
- **FR-22.** 학습 데이터 정리(보존 기간 선택 삭제)와 누적 기록 초기화를 제공한다.
- **FR-23.** 로그아웃/계정 관리, 앱 버전 정보를 제공한다.

### 9.6 알림 (최소)
- **FR-24.** 로컬 학습 리마인더(예: "오늘도 5분 말하기?")를 로컬 알림으로 제공한다. (서버 푸시는 v1.1)
- **FR-25.** Android 13+(API 33+)에서만 POST_NOTIFICATIONS 권한을 맥락과 함께 조건부 요청한다(그 이하 버전은 권한 불필요).

### 9.7 사용량 한도 (비용 통제 & 미래 과금 레버)
- **FR-26.** 무료 사용자는 **하루 N세션(기본 3)** 한도 내에서 대화 학습을 시작할 수 있다. **v1에서 한도 도달 시 비상업적 중립 문구**("오늘 무료 학습을 다 했어요 — 내일 또 만나요" + 스트릭 넛지)를 표시하며, **업그레이드 CTA·가격은 노출하지 않는다.** (업그레이드 유도 자리는 v1.1 훅으로만 남겨둔다.)
- **FR-27.** 한도 카운트·리셋은 **백엔드(Functions)에서** 판정한다(클라이언트 신뢰 금지 — 카운터는 서버 전용 쓰기, §10.4·NFR-1).

---

## 10. 기술 아키텍처

### 10.1 클라이언트 (Android)
- **언어/UI:** **Kotlin 2.x (JVM/JDK 타깃 17)**, **Jetpack Compose + Material 3**. 단일 Activity.
- **아키텍처:** MVVM/MVI + Kotlin Coroutines/Flow. DI는 **Hilt**.
- **미디어:** 오디오 녹음(16kHz PCM) · TTS 재생(AudioTrack) · (필요 시 Media3).
- **커스텀 뷰 → Compose Canvas 재구현:** 벤다이어그램(대비 가드 포함), 실시간 파형, 슬롯머신 카운트업.
- **스트리밍 파싱 위치:** **점진적 파싱은 백엔드(Functions)에서만 수행하고, 완성된 객체(턴/섹션/문제)를 SSE 이벤트로 emit한다. 클라이언트는 원시 JSON을 파싱하지 않고 수신 객체를 렌더만 한다.** (상태 보존은 ViewModel + SavedState.)

### 10.2 백엔드 (greenfield)
- **Firebase 새 프로젝트:** Auth(Anonymous + Google), **Firestore**(사용자 데이터), Analytics, (v1.1) FCM.
- **Cloud Functions = 서버 로직 계층:**
  - **LLM 프록시(필수):** 모든 Gemini 호출을 중계. **API 키는 서버에만 보관.** rate-limit·일일 한도 판정·남용 차단·비용 모니터링·프롬프트 캐싱·**점진 파싱 후 완성 객체 SSE emit.** 스트리밍을 위해 **2nd-gen Cloud Functions(Cloud Run 기반) 스트리밍 응답**을 사용하고, **min-instances/워밍으로 콜드스타트를 완화**한다(NFR-3 보호).
  - **세션 요약 파이프라인:** 어휘 추출·표현 필터링.
  - (v1.1) 구독 검증, 서버 푸시.

### 10.3 AI / LLM
- **v1은 올-Gemini:** 텍스트 생성(Flash 계열) · 네이티브 오디오로 전사+prosody · Gemini TTS.
- **프로바이더 추상화 시임:** Functions의 LLM 프록시는 벤더 중립 인터페이스로 설계한다. 향후 "피드백 생성만 Claude로" A/B하거나 통째로 교체할 때 **클라이언트 변경 0**. (Claude는 텍스트(+이미지) 전용 — 오디오/TTS는 항상 Gemini 담당.)
- **점진적 스트리밍 파싱:** **백엔드에서** 부분 JSON에서 완성된 객체만 추출(대본 턴/피드백 섹션 단위)해 클라이언트로 emit.
- **프롬프트 톤(프롬프트로 강제):** 해요체, 문법 전문용어 금지, 혜택 우선, 2줄 이내, 교사가 아닌 파트너. 한국인 학습자 8대 오류 유형을 레퍼런스로 내장.

### 10.4 데이터 모델 (Firestore, greenfield 초안)
```
users/{uid}
 ├─ profile            (nickname, level, createdAt, isGuest, linkedProvider)
 ├─ saved_cards/{id}   (type: word|expression|sentence, payload, savedAt)
 ├─ gamification/state (xp, studyTimeTotal, studyDays, streak, lastStudyDate)
 ├─ gamification/points/{sessionId}   (멱등 적립 원장)
 └─ usage/{yyyymmdd}   (sessionCount — 일일 한도 판정 / Functions 전용 쓰기)
sessions/{sessionId}   (선택: 세션 메타/요약 캐시)
config/                (서버 소유: 주제 프리셋, 한도 값, 프롬프트 버전)
```

**동기화 정책 (per-field):** 진실원천은 **Firestore(서버)**. 로컬(DataStore)은 오프라인 캐시 + write-ahead 큐.
- **XP·학습시간·학습일:** 세션 ID 멱등 원장에 1회 기록 → 서버 트랜잭션으로 집계 증분(오프라인 재생 시에도 중복 적립 없음).
- **streak:** `lastStudyDate` 기준 **서버 재계산**(맹목 머지 금지).
- **saved_cards:** `cardId` union, 삭제는 tombstone(삭제 우선).
- **usage/{yyyymmdd}:** **Functions 전용 쓰기**(클라이언트 쓰기 불가, 보안규칙 carve-out — NFR-1).

상세 스키마·보안 규칙·멱등 집계 트리거·게스트 이관 흐름은 [docs/design/firestore-schema.md](docs/design/firestore-schema.md)에 확정(v3). 위 트리는 초기 스케치이며, 충돌 시 설계 문서가 우선한다(예: 게임화는 `gamification/state` 단일 문서 → `gamification/progress`(서버) + `gamification/studytime`(클라) + `point_ledger`·`progress_marks`로 분리됨).

---

## 11. 디자인 / UI 시스템

- **디자인 언어:** "정제된 미니멀리즘"(토스풍) **클린 재구축**. 플랫(elevation 0) · 라운드(4~24dp 체계) · 넉넉한 여백 · 볼드 헤딩 · 큰 탭 타깃.
- **컬러:** **고정 브랜드 팔레트**(주조색 토스 블루 #39A0ED). 다이내믹 컬러(머티리얼 유) **미사용** — 점수·음성 4상태·스트릭 등 *의미 색*을 보존하기 위함.
  - **라이트/다크 완전 토큰셋:** 고정 팔레트는 다크 변형 토큰(`values-night` 등가)을 Compose 테마에 함께 정의(NFR-5 충족).
  - 레거시 정리: 시맨틱 토큰 체계(Compose) 정립, 잘못된 네이밍(`purple_500`=코랄 등) 폐기, 하드코딩 hex 박멸, 영어 잔존 문자열 100% 한글화.
- **토큰 명세 확정:** 라이트/다크 전 토큰셋·의미 색·레거시 매핑·타이포/간격/코너 스케일은 [docs/design/design-tokens.md](docs/design/design-tokens.md)에 분리 확정.
- **타이포:** Pretendard 전역 적용, sp 단위, 큰 위계(화면 제목 28sp Bold 등).
- **시그니처 인터랙션 유지:** 음성 4상태 · 실시간 파형(crackle) · 슬롯머신 카운트업(스프링 반동) · 점진적 스켈레톤(시머).
- **상태 디자인:** 스켈레톤+시머로 AI 응답 점진 표시, 로딩 카피 4단계 회전, 따뜻한 빈 상태, 비난 없는 에러("불러오지 못했어요" + 다시 시도).
- **인터랙션 무대:** 주요 행위는 바텀시트 위에서(맥락 유지, 화면 전환 비용↓).
- **정보 구조(v1):** 하단 탭 축소 — **학습(홈) · 기록 · 설정** 3탭(쇼츠·게임 탭 제거). 부가 화면은 탭이 아닌 진입.

---

## 12. 비기능 요구사항 (NFR)

- **NFR-1. 보안:** LLM API 키는 절대 클라이언트에 두지 않는다(백엔드 프록시 강제). Firestore 보안 규칙로 사용자 데이터 격리. **`usage/{date}`는 Functions 전용 쓰기(클라이언트 읽기전용)로 규칙에서 carve-out**해 한도 우회를 차단한다.
- **NFR-2. 비용:** 일일 한도 + 프롬프트 캐싱 + 스트리밍 점진 렌더 + transcript 재사용(중복 인식 제거)으로 LLM 비용·지연 최적화. 백엔드에서 사용량/비용 모니터링.
- **NFR-3. 성능/체감:** 첫 토큰까지 빠르게(스트리밍), 턴 피드백 슬림화로 대기 체감 최소화. 목표 턴 응답 5~9초 이하. **프록시 hop·콜드스타트는 2nd-gen 스트리밍 + min-instances로 완화**(§10.2).
- **NFR-4. 오프라인:** 핵심 루프는 온라인 필수. 오프라인 시 저장 카드·기록·게임화 통계 **열람**은 가능, 명확한 오프라인 에러. (쓰기는 §10.4 write-ahead 큐로 복귀 시 동기화.)
- **NFR-5. 접근성:** 실용적 **AA 지향** — 48dp+ 터치, sp 텍스트, 라이트/다크 완전 대응, 벤다이어그램 런타임 대비 가드, 아이콘 contentDescription. (AAA 전수는 비목표)
- **NFR-6. 신뢰성:** 멱등 적립, 비동기 경쟁 차단, 무음 감지, TTS 워치독, 토큰 기반 콜백 보호.
- **NFR-7. 계측:** Firebase Analytics로 핵심 퍼널 이벤트(세션 시작·완주, 턴 수, 저장, 한도 도달)와 D1/D7 리텐션 코호트 추적.
- **NFR-8. 테스트:** 위험지대 집중 — **백엔드 스트리밍 파서**, 세션 오케스트레이션, 게임화 멱등, 피드백/요약 스키마 검증, 동기화 머지 정책, 대비 가드. 단위 + 핵심 플로우 계측 테스트. 커버리지 강박 없음.
- **NFR-9. 최소 지원:** **minSdk 26 (Android 8.0) 확정** — Compose/M3(≥21)·Credential Manager + Google ID(26 미만 호환) 요건 충족, 기기 ~95%+ 커버. targetSdk 36. (구 앱은 31이었으나 이는 Shorts/media3 등 자체 선택 탓이며 v1은 이를 컷.) 보안·라이브러리 요건이 바뀌면 상향 가능.

---

## 13. 수익화 (보류 + 미래 방향)

- **v1:** 수익화 없음. 무료 + 일일 한도(기본 3세션)로 비용 통제 + 제품 검증. **과금 UI(가격·업그레이드 CTA)는 출하하지 않는다.**
- **v1.1+ 방향:** **구독(월정액)** — 무제한/고한도 해제. **크레딧·리워드 광고·쿠폰·소비성 결제·환불 클로백은 채택하지 않는다**(1인 운영 부담 과다, 그라인드 UX).
- 일일 한도는 미래 전환 레버로 설계됨("무제한은 업그레이드") — 단 그 UI는 §6의 검증 게이트(D7 20%) 도달 후 v1.1에서 켠다.

---

## 14. 릴리즈 계획 (마일스톤)

| 단계                     | 내용                                                                                                                        |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| **M0 — 기반**            | Compose/M3 프로젝트 스캐폴드(**minSdk 26 확정**), Firebase 새 프로젝트, Cloud Functions LLM 프록시(추상화 시임, 2nd-gen 스트리밍), 디자인 토큰/테마(라이트·다크) |
| **M1 — 핵심 루프**         | 대본 생성(스트리밍) → 대화 턴(녹음·음성4상태·TTS) → 슬림 피드백. **세션 진입은 내부 개발 하니스**(하드코딩 진입·한도 게이트 없음, **빌드 플래그 뒤**, 비사용자노출)                  |
| **M2 — 요약 & 저장**       | 세션 요약 파이프라인, 깊은 분석(온디맨드), 저장 카드·기록 탭                                                                                      |
| **M3 — 온보딩 & 인증 & 한도** | 게스트 시작, 보장된 첫 세션, Google 연결(FR-3/3a/3b), 일일 한도(FR-26/27), 게임화(XP·스트릭·학습시간), 로컬 리마인더. **사용자 대면 진입 경로가 M1 하니스를 대체**         |
| **M4 — 마감**            | 계측·접근성·에러/빈/로딩 상태, QA, 베타                                                                                                 |
| **v1.1(후속)**           | 복습 퀴즈, 구독 수익화, FCM 서버 푸시/알림함, (검토) 피드백 Claude A/B, 이메일 로그인                                                                |

---

## 15. 리스크 & 미해결 질문 (Open Questions)

- **R1. 슬림 피드백의 차별성:** 턴 피드백을 가볍게 하면 "그냥 챗봇"처럼 보일 위험. → 깊은 분석을 요약/온디맨드에서 충분히 강하게 보여줘 차별성 유지 필요. (검증 필요)
- **R2. 대본형의 한계:** "번역 훈련"처럼 느껴질 수 있음. → 상대역 캐릭터·상황 몰입과 자연스러운 첨삭으로 보완. 자유 대화는 v2 검토.
- **R3. prosody 점수 신뢰도:** LLM의 유창성/자신감 점수가 불안정할 수 있음. → **v1은 음성/prosody 숫자 점수를 내지 않는다**(정성 "한 줄 격려"만) — 위험을 설계로 회피. 숫자는 텍스트 작문 점수만 제공.
- **R4. 일일 한도(3) 적정선:** 비용 vs 학습 충분성. → 출시 후 데이터로 보정.
- **OQ1.** ~~minSdk 확정~~ → **해결: minSdk 26 (Android 8.0) 확정** (NFR-9).
- **OQ2.** ~~주제 큐레이션 셋(12~20개) 최종 선정~~ → **해결: 300개·4그룹(각 75개)·6 beginnerFriendly**, 로컬 에셋 [android/app/src/main/assets/topics.json](android/app/src/main/assets/topics.json) 및 [카탈로그 설계](docs/design/topic-catalog.md) (grill-review deep auto, 0B SHIP).
- **OQ3.** ~~TTS 음성/로케일 기본값, 단말 TTS 폴백 범위~~ → **해결: [docs/design/tts.md](docs/design/tts.md)** (Gemini TTS·2음성 성별 매핑·en-US·1.0x·워치독 8초·조건부 단말 폴백, grill-review 0B SHIP).
- **OQ4.** ~~Firestore 상세 스키마 & 보안 규칙 설계~~ → **해결: [docs/design/firestore-schema.md](docs/design/firestore-schema.md)** (grill-review deep auto로 경화, v3.1 SHIP — 게임화 값[XP 10/20/35·한도 3·streak 완주일+1일 유예]·완주 정의 확정 포함).

> **미해결 질문 OQ1~OQ4 전부 해결됨** — 상세 설계는 [docs/design/](docs/design/) 참조. (R1~R4는 진행 중 리스크로 유지.)

---

## 부록 A — 의사결정 로그 (인터뷰 15문항)

| # | 결정 | 선택 |
|---|---|---|
| 1 | 재구축 성격 | 기술 현대화 + 핵심 루프 **재집중** (기능 동등 아님) |
| 2 | 플랫폼 | **Android 전용** v1 |
| 3 | 스택 | **Kotlin + Compose + Material 3** (단일 Activity, MVVM/MVI, Coroutines/Flow, Hilt) |
| 4 | 타깃 | 말하기에 자신 없는 **기초~중급(A2~B1) 성인** |
| 5 | 상호작용 모델 | **대본형 가이드 롤플레이** (자유 대화 아님) |
| 6 | 피드백 깊이 | 턴은 **슬림**, 깊이는 **요약/온디맨드** |
| 7 | 범위선 | 핵심 루프 IN / 퀴즈·푸시 보류 / 쇼츠·게임탭 컷 |
| 8 | 백엔드 | **greenfield**(기존 데이터 미승계) |
| 9 | 백엔드 스택 | **Firebase 새 프로젝트 + Cloud Functions**, LLM **백엔드 프록시** |
| 10 | AI 벤더 | **올-Gemini** v1 + **프로바이더 추상화 시임** |
| 11 | 수익화 | **무료 + 일일 한도(3)**, 크레딧/광고/쿠폰/결제 컷, 구독은 v1.1 |
| 12 | 인증 | **게스트(Anonymous) → Google** 업그레이드, 이메일 컷 |
| 13 | 디자인 | **토스풍 클린 재구축**, 고정 팔레트(다이내믹 컬러 OFF), 시그니처 유지 |
| 14 | 성공 지표 | **D7 리텐션 20%(확정 게이트)** + 완주율(~70%) + 주당 세션(~3) |
| 15 | 온보딩 | **초경량 2문항 + 보장된 승리 첫 세션(5턴)** |

**가정(별도 그릴 없이 기본값 채택):** 레벨 자가선택(3단계, 플레이스먼트 없음) · 주제 로컬 300개(4그룹·온보딩 6개)+직접입력 · 세션 5/10턴(첫 세션 5턴 고정) · Gemini TTS 기본 · 오프라인 열람만 · UI 한국어 전용 · 실용 AA · greenfield Firestore · Analytics 계측 · 위험지대 집중 테스트 · 게임화 간소화 · 점진 상태 디자인.

---

## 부록 B — 변경 이력 (v0.1 → v0.2, grill-review deep auto 반영)

격리 컨텍스트의 리뷰어 2종(fact-checker + critic)이 `archive/android` ground truth와 라이브러리/API 현실에 대조해 검토. Blocker 4 + Advisory 10을 해소하고 재검증(iteration 2)에서 SHIP 도달.

| 항목 | 변경 |
|---|---|
| **B1** (Blocker) | 스트리밍 파싱 위치 모순 해소 → **백엔드 단독 파싱, 완성 객체 SSE emit, 클라이언트는 렌더만** (§8.2.1, §10.1, §10.3, NFR-8, FR-6) |
| **B2** (Blocker) | 클라우드 동기화 "간소화" 미정의 → **per-field 정책 명시**(멱등 원장/union/tombstone/서버 재계산) (§8.4, §10.4) |
| **B3** (Blocker) | 마일스톤 순서 역전 → **M1 진입은 빌드 플래그 뒤 내부 하니스, M3 사용자 진입이 대체** (§14) |
| **B4** (Blocker) | 게스트→Google 승계가 복귀 사용자에겐 비자동 → **FR-3a(인플레이스)/FR-3b(충돌 시 명시 이관) 분기** (§9.1) |
| A2 | 무수익화 비목표 vs v1 업그레이드 자리 → **한도 도달 시 중립 문구, 과금 UI 미출하** (FR-26, §13) |
| A3 | 한도 카운터 위치 → **`usage`는 Functions 전용 쓰기 carve-out** (§10.4, NFR-1, FR-27) |
| A4 | minSdk 미확정 → **minSdk 26 확정** (NFR-9, OQ1, M0) |
| A5 | "Kotlin 17+" 오기 → **Kotlin 2.x (JDK 17)** (§10.1) |
| A6 | "검증됨" 게이트 부유 → **D7 20% 확정 게이트** (§6) |
| A7 | 온보딩 레벨 폐기 → **profile.level 저장, 세션 #2부터 적용** (§8.1, FR-2) |
| A8 | 점수 신뢰도 모순 → **텍스트 작문 점수만 숫자, 음성은 정성 격려(숫자 없음)** (§8.2.2, §8.3, FR-10, R3) |
| A9 | 다크 토큰 미정의 → **고정 팔레트 다크 변형 토큰셋 명시** (§11) |
| A10 | SSE 프록시 지연 미인지 → **2nd-gen Functions 스트리밍 + 콜드스타트 완화** (§10.2, NFR-3) |

---

*이 문서는 v1.0(Confirmed)다(인터뷰 초안 + 객관 리뷰 + OQ1~4 전부 해결 + 상세 설계 산출물 완비). 상세 설계는 [docs/design/](docs/design/)에 분리 확정: 데이터 스키마([firestore-schema.md](docs/design/firestore-schema.md))·TTS([tts.md](docs/design/tts.md))·프롬프트 시스템([prompt-system.md](docs/design/prompt-system.md) + [prompts/](docs/design/prompts/))·주제 카탈로그([topic-catalog.md](docs/design/topic-catalog.md))·디자인 토큰([design-tokens.md](docs/design/design-tokens.md)). 남은 것은 출시 후 데이터로 검증할 제품 리스크(§15 R1~R4)뿐이며, 이는 문서 공백이 아니다.*
